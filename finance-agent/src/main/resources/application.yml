server:
  port: 8081

spring:
  ai:
    openai:
      api-key: ${DEEPSEEK_API_KEY:your-api-key-here}
      base-url: https://api.deepseek.com/v1
      chat:
        options:
          model: deepseek-chat
    mcp:
      client:
        connections:
          finance-mcp:
            type: SYNC
            url: http://localhost:8082
