server:
  port: 8081

spring:
  ai:
    openai:
      api-key: ${DEEPSEEK_API_KEY:your-api-key-here}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
    mcp:
      client:
        type: SYNC
        sse:
          connections:
            finance-mcp:
              url: http://localhost:8082
