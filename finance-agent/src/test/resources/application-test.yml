spring:
  ai:
    openai:
      api-key: ${LLM_API_KEY}
      base-url: ${LLM_BASE_URL}
      chat:
        options:
          model: ${LLM_MODEL:deepseek-chat}
          reasoning-effort: low
          max-tokens: 1024
    mcp:
      client:
        type: SYNC
        sse:
          connections:
            finance-mcp:
              url: http://localhost:8082

finance:
  memory-dir: target/test-memory
  audit:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health
